var LiveReloadPlugin = require('webpack-livereload-plugin');

module.exports = {
    module: {
        rules: [
            {
                test: /\.js$/,
                exclude: /node_modules/,
                use: {
                    loader: "babel-loader"
                }
            }
        ]
    },
    plugins: [
        new LiveReloadPlugin()
    ]
};